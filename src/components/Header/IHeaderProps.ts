export interface IHeaderProps {
  containerStyle?: any; // Overriding options for styling.
  linearStyle?: any;
  onpress?: any;
  isAvatar?: boolean;
  rightIconSource: any;
  rewardPoints?: string;
}
